import type { AppBase, Entity } from 'playcanvas';

export class EnvironmentManager {
  public static populate(_app: AppBase, _worldRoot: Entity, _width: number, _depth: number): void {
    // Intentionally empty for now.
    // The battlefield should stay clean while the core visual direction is being refined.
  }
}
